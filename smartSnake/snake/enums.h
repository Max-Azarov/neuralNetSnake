#ifndef ENUMS_H
#define ENUMS_H

enum TYPE_CELL
{
    EMPTY = 0,
    BODY = 1,
    WALL = 2,
    FRUIT = -16,
    HEAD = 8,
};

enum DIRECTION
{
    UP = 0,
    LEFT,
    DOWN,
    RIGHT
};

#endif // ENUMS_H
